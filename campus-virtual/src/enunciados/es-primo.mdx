
Debes crear una función llamada `es_primo(numero)` que tome un número entero positivo y devuelva `True` si el número es primo, y `False` en caso contrario.

Recuerda que un número primo es un número natural mayor que 1 que tiene únicamente dos divisores positivos distintos: él mismo y el 1.




### *Código Base*:

<CodeBlock language="python" title="es primo">
{`
def es_primo(numero):
    pass
`}
</CodeBlock>