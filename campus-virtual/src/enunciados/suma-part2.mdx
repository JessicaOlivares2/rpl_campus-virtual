// src/enunciados/suma-part2.mdx

# Enunciado

Debes implementar una función llamada `sumar(a, b)` que devuelva la suma de los dos parámetros. El código será validado con el archivo de pruebas `test_suma_simple.py`.

### Código Base:

<CodeBlock language="python" title="def sumar(a, b):">
{`
def sumar(a, b):
    pass
`}
</CodeBlock>