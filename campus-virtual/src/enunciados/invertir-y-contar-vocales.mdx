Debes implementar una función llamada procesar_cadena(s) que reciba una cadena de texto (s).

La función debe realizar dos tareas y devolver el resultado como una tupla en el siguiente formato: (cadena_invertida, cantidad_vocales).

Invertir la Cadena: La primera parte de la tupla debe ser la cadena de texto de entrada (s) completamente invertida.

Contar Vocales: La segunda parte de la tupla debe ser el número total de vocales (a, e, i, o, u, contando tanto mayúsculas como minúsculas) presentes en la cadena original.

El código será validado con el archivo de pruebas test_procesar_cadena.py.

Ejemplo:
Entrada: "Algoritmo"

Salida Esperada: ("omtiroglA", 4)

### Código Base:

<CodeBlock language="python" title="def sumar(a, b):">
{`
def procesar_cadena(s):
    pass
`}
</CodeBlock>